query RecipeList(
  $types: [RecipeType!]
  $category: DishCategory
  $method: BrewMethod
  $favorite: Boolean
  $sort: RecipeSort
  $order: SortOrder
  $limit: Int
  $after: RecipeId
) {
  recipes(
    types: $types
    category: $category
    method: $method
    favorite: $favorite
    sort: $sort
    order: $order
    limit: $limit
    after: $after
  ) {
    items {
      id
      title
      type
      category
      method
      favorite
      updatedAt
      versionCount
      toTestCount
      bestRating
    }
    hasMore
    totalCount
  }
}
